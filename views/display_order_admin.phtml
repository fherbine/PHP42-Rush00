<?php
	$orders = get_orders(BDD_PATH, BDD_ORDER);
	//$articles = get_articles(BD_PATH, BDD_ARTICLE);
	foreach ($orders as $login => $order) :;
?>
<tr>
    <td><?= $login ?></td>
    <td><?= $order['art_title'] ?></td>
    <td><?= $order['art_quant'] ?>€</td>
</tr>
<?php endforeach ?>
